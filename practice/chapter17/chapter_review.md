# 1.C++把数据的输入\输出以字节信息流的形式进行处理。通过类的继承关系，C++定义了用于数据输入和输出的类。其中核心是istream和ostream

# 2.标准化输入工具将键盘输入的信息按照字符形式读取到数据缓冲区，程序自动将字符转换为相应数据类型 的二进制表示/

# 3. 默认情况下。标准输出和标准错误都将连接标准输出设备。如果是要求操作系统将输出重定向到文件，则标准输出与文件相连，而标准错误仍与显示器相连/

# 4. 因为C++语言在标准输入/输出中为ostream类中已经为C++的基本类型定义了operator<<()运算符重载函数。对于用户自定义类型，则需要类定义时手动重载operator<<()运算符。

# 5.在重载operator<<()运算符时，返回ostream&类型对象。当该对象调用输出运算符时则会返回该对象本身，继续调用下一个输出。

# 8. ct1 = 5 , ct2 = 9

# 9. 第一种get方法会每次读取一个字符，包括空字符，读取后就丢弃直到遇到第一个回车符。ignore（80,'\n）最多读区80个字符并丢弃或者遇到第一个回车符，两者与先遇到为准